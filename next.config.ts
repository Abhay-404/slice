import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // No floating dev badge — it sits on top of the mic orb.
  devIndicators: false,
  /* config options here */
};

export default nextConfig;
